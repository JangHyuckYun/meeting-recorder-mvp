# 2026-09-01 작업 내역

작업관리 DB: https://app.notion.com/p/c20d684f5dc04466a4d9f4ab771def7e (row별 상세 로그는 Notion)

## 머지됨 (main)
- **PR #1** — 화자 수 옵션(가져오기/실시간, 마이그레이션 0007), LLM 설정(모델별 reasoning/fast, 마이그레이션 0008, 최신 OpenAI/Anthropic 목록, `GET /v1/models` 불러오기), 외부 공급자에 마스킹된 키를 보내던 버그 수정, 노트 탭 레이아웃(숨김 Radix 패널) 수정, MCP 서버 `apps/mcp`(`meeting-transcribe`: list_providers / configure_provider / transcribe, `speakers: "auto"|N`), `AGENTS.md`, `error-log.md`.

## 열린 PR (base: main)
- **PR #2** `feat/note-template-tabs` — 회의록 생성 시 템플릿 선택, 스크립트 탭 → "대화 기록(스크립트)" 통합.
- **PR #3** `feat/import-eta-persist` — 전사 예상 남은 시간, 화면 이동 후에도 진행상태 유지(`importStore` + `get_active_transcriptions`), 전사 중 홈 → 가져오기 이동.
- **PR #4** `feat/settings-provider-oauth` — STT 기본값 ElevenLabs(원인: Rust 기본값 `self_hosted`; 기존 DB는 마이그레이션 0010으로 전환), 서버 URL 조건부, 설정의 화자 수 제거, 공급자 OpenAI / Anthropic / vLLM(OpenAI 호환) 분리, API key / OAuth(마이그레이션 0009 `auth_mode`), 인앱 OAuth 로그인(PKCE, 브라우저, localhost 콜백, 붙여넣기 폴백).
- **PR #5** `fix/elevenlabs-keyterms` — 용어집 잘못된 항목으로 ElevenLabs 400 `invalid_keyword` 전체 실패 → 전송 전 정규화, 실패 시 용어 없이 1회 재시도, 단어장 저장 검증. (작성 중)
- 세 PR 모두 `apps/desktop/CHANGELOG.md`를 새로 만들어 두 번째 머지부터 충돌 → 머지 순서대로 리베이스 필요. 로컬 통합 브랜치 `integration/pr2-3-4`(미푸시)로 앱 동작 확인 중.

## 조사 결론
- **전사 품질("8월 31일 팀 주간회의")**: 녹음(16kHz mono WAV)은 정상. 실제 엔진이 self_hosted WhisperLive였음(설정 표시 버그). 같은 WAV를 ElevenLabs Scribe v2로 재전사 → 338개 파편/의미 불명 → 155개 세그먼트/자연스러운 문장. 결과 `~/Downloads/0831_주간회의_elevenlabs/`. 부가 후보(미검증): `afconvert` 품질 플래그.
- **OAuth**: 앱은 CLI 자격증명 파일을 읽고 refresh만 했음. omo(pi-ai) 흐름과 동일 client_id/토큰 엔드포인트라 기존 파일 포맷에 그대로 기록하는 방식으로 인앱 로그인 구현. 배포 시 client_id 약관 확인 필요.

## 검증 기준(모든 PR 공통)
`pnpm exec tsc --noEmit` · `pnpm vitest run` · `cargo check && cargo test` — 오케스트레이터가 직접 재실행해 확인.

## 수동 확인 대기
- 설정 재진입 시 ElevenLabs 표시 / OAuth 로그인 브라우저 왕복 / 가져오기 ETA·상태 유지 / 템플릿 선택 생성.
